\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    node distance=1.2cm,
    block/.style={draw, rectangle, minimum height=2.2em, minimum width=8em, align=center, rounded corners, fill=teal!5, scale=0.85},
    arrow/.style={draw, -latex', thick}
]
    \node [block] (scheduler) {Worker Scheduler};
    \node [block, right=of scheduler, yshift=1.2cm, xshift=1cm] (w1) {Engine Worker 1};
    \node [block, right=of scheduler, xshift=1cm] (w2) {Engine Worker 2};
    \node [block, right=of scheduler, yshift=-1.2cm, xshift=1cm] (w3) {Engine Worker N};

    \draw [arrow] (scheduler) -- (w1);
    \draw [arrow] (scheduler) -- (w2);
    \draw [arrow] (scheduler) -- (w3);
\end{tikzpicture}
\caption{Parallel Worker thread scheduling model for multi-game execution.}
\label{fig:parallel_worker_architecture}
\end{figure}
